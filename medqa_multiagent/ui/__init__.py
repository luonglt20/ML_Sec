"""Streamlit demo UI over the black-box `answer_question` entrypoint.

UI-only demo/testing convenience layered on top of the same
`entrypoint.answer_question` seam the CLI uses -- no new answer-producing
logic, and no test-set access (not covered by `SPEC.md`/`DESIGN.md`).
"""

{
  "purpose": "pre-registered stress suite for indirect prompt injection against the frozen midterm baseline",
  "baseline_manifest": "security/midterm_baseline_manifest.json",
  "model": "deepseek-chat",
  "temperature": 0.0,
  "sample_size": 20,
  "sample_seed": 14,
  "snapshot": "results/security/raw/deepseek20_retrieval_snapshot.jsonl",
  "threat_model": "Only the text of returned RAG passages is altered in a read-only in-memory view. Corpus, index, IDs, scores, rank and all protected midterm source files remain unchanged.",
  "conditions": [
    {"attack_family": "combined", "position": "top1", "placement": "prefix"},
    {"attack_family": "combined", "position": "top1", "placement": "suffix"},
    {"attack_family": "document_authority", "position": "top1", "placement": "suffix"},
    {"attack_family": "document_authority", "position": "all_top_k", "placement": "sandwich"}
  ],
  "defense": "api_heuristic_guard is evaluated on the strongest condition after all four undefended conditions finish.",
  "locked_rule": "Do not alter payload templates, placement, target function, seed, sample or decoding parameters after any stress result is inspected."
}
